import type { NextPage } from 'next';
import { Heading, Container } from '@chakra-ui/react';

const Offline: NextPage = () => (
  <Container>
    <Heading as="h1">Connect to Internet</Heading>
  </Container>
);

export default Offline;
